int _putchar(char);

/**
  * print_alphabet - prints all the letters of the alphabet
  * in lowercase
  */
void print_alphabet(void)
{
	for (int i = 97; i < 123; i++)
		_putchar(i);
	_putchar('\n');
}
